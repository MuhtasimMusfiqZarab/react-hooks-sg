// this is the first file to be rendered

import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

//rendering to app
ReactDOM.render(<App />, document.querySelector("#root"));
